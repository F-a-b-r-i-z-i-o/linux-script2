# linux-script2

Bash script that takes "k> = 2" input filenames and appends the first "k-1" files 
in the file passed as the last argument (k-th), 
writing the input files in order from right to left.
